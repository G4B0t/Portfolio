import { ArrowUpRight } from 'lucide-react'; import { Link } from 'react-router-dom'; import styled from 'styled-components'; import { Badge } from '@/components/ui/Badge'; import { TechBadge } from '@/components/common/TechBadge'; import type { ProjectDefinition } from '@/types/project';
const Card = styled.article`display:grid;overflow:hidden;border:1px solid ${({ theme }) => theme.colors.border};border-radius:${({ theme }) => theme.radii.lg};background:${({ theme }) => theme.colors.surface};box-shadow:${({ theme }) => theme.shadows.card};transition:transform ${({ theme }) => theme.transitions.default},border-color ${({ theme }) => theme.transitions.default};&:hover{transform:translateY(-4px);border-color:${({ theme }) => theme.colors.accent};}`; const Visual = styled.div`min-height:10rem;padding:${({ theme }) => theme.spacing[5]};background:linear-gradient(135deg,${({ theme }) => theme.colors.surfaceElevated},${({ theme }) => theme.colors.background});border-bottom:1px solid ${({ theme }) => theme.colors.border};color:${({ theme }) => theme.colors.accent};font-family:${({ theme }) => theme.typography.display};display:flex;align-items:flex-end;`; const Body = styled.div`display:grid;gap:${({ theme }) => theme.spacing[3]};padding:${({ theme }) => theme.spacing[5]};`; const Title = styled.h3`margin:0;font-size:${({ theme }) => theme.typography.sizes.xl};`; const Copy = styled.p`margin:0;color:${({ theme }) => theme.colors.textMuted};`; const Meta = styled.div`display:flex;flex-wrap:wrap;gap:${({ theme }) => theme.spacing[2]};`; const Read = styled(Link)`display:inline-flex;align-items:center;gap:.35rem;width:fit-content;color:${({ theme }) => theme.colors.accent};font-weight:700;`;
export function ProjectCard({ project }: { project: ProjectDefinition }) { return <Card><Visual aria-hidden="true">{project.category}</Visual><Body><Badge>{project.subtitle}</Badge><Title>{project.title}</Title><Copy>{project.summary}</Copy>{project.technologies.length > 0 && <Meta>{project.technologies.map((technology) => <TechBadge key={technology} label={technology} />)}</Meta>}<Read to={`/work/${project.slug}`} aria-label={`View ${project.title} case study`}>View foundation <ArrowUpRight size={16} /></Read></Body></Card>; }
