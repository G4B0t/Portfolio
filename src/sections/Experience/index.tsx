import styled from 'styled-components'; import { SectionHeader } from '@/components/common/SectionHeader'; import { Container } from '@/components/ui/Container'; const Section = styled.section`padding:${({ theme }) => theme.spacing[8]} 0;`; export function Experience() { return <Section><Container><SectionHeader eyebrow="Experience" title="Context before claims.">Professional experience details are intentionally being validated before publication.</SectionHeader></Container></Section>; }
