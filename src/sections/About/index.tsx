import styled from 'styled-components'; import { SectionHeader } from '@/components/common/SectionHeader'; import { Container } from '@/components/ui/Container'; import { profile } from '@/content/profile'; const Section = styled.section`padding:${({ theme }) => theme.spacing[8]} 0;background:${({ theme }) => theme.colors.surface};`; export function About() { return <Section id="about"><Container><SectionHeader eyebrow="About" title="Built around the work.">{profile.about}</SectionHeader></Container></Section>; }
