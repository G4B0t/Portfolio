import styled from 'styled-components'; import { TechBadge } from '@/components/common/TechBadge'; import { SectionHeader } from '@/components/common/SectionHeader'; import { Container } from '@/components/ui/Container'; import { skills } from '@/content/skills'; const Section = styled.section`padding:${({ theme }) => theme.spacing[8]} 0;background:${({ theme }) => theme.colors.surface};`; const List = styled.div`display:flex;flex-wrap:wrap;gap:${({ theme }) => theme.spacing[2]};margin-top:${({ theme }) => theme.spacing[5]};`; export function Skills() { return <Section id="skills"><Container><SectionHeader eyebrow="Focus areas" title="A practical engineering range.">Areas that guide the portfolio’s technical direction.</SectionHeader><List>{skills.map((skill) => <TechBadge key={skill} label={skill} />)}</List></Container></Section>; }
