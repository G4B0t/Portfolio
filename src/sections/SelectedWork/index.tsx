import styled from 'styled-components'; import { ProjectCard } from '@/components/common/ProjectCard'; import { SectionHeader } from '@/components/common/SectionHeader'; import { Container } from '@/components/ui/Container'; import { featuredProjects } from '@/content/projects';
const Section = styled.section`padding:${({ theme }) => theme.spacing[8]} 0;`; const Grid = styled.div`display:grid;gap:${({ theme }) => theme.spacing[4]};margin-top:${({ theme }) => theme.spacing[6]};@media(min-width:${({ theme }) => theme.breakpoints.tablet}){grid-template-columns:repeat(2,minmax(0,1fr));}`;
export function SelectedWork() { return <Section id="work"><Container><SectionHeader eyebrow="Selected work" title="Projects with intent.">A growing collection of work, with public-facing details added only after careful review.</SectionHeader><Grid>{featuredProjects.map((project) => <ProjectCard key={project.slug} project={project} />)}</Grid></Container></Section>; }
