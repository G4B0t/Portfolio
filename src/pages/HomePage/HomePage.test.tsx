import { render, screen } from '@testing-library/react'; import { MemoryRouter } from 'react-router-dom'; import { ThemeProvider } from '@/app/providers/ThemeProvider'; import { HomePage } from './index';
describe('HomePage', () => { it('renders selected projects from centralized content', () => { render(<ThemeProvider><MemoryRouter><HomePage /></MemoryRouter></ThemeProvider>); expect(screen.getByRole('heading', { name: 'Projects with intent.' })).toBeInTheDocument(); expect(screen.getByRole('heading', { name: 'PMCA' })).toBeInTheDocument(); expect(screen.getByRole('heading', { name: 'Viva Aerobus' })).toBeInTheDocument(); expect(screen.getByRole('heading', { name: 'GLI Simulator' })).toBeInTheDocument(); }); });
